name: 'Build Package(s)'

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
        - name: Checkout Repository
          uses: actions/checkout@v4

        - name: Build CP_Package
          run: bash ${GITHUB_WORKSPACE}/CP_Source/CP_Packages/Apps/LVSI_Custom_Web_Connector/build/build-lvsi-custom-web-connector-cp.sh'

